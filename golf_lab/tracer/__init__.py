"""Ball tracer: the drawn flight line and the data behind it.

Milestone 5 is the manual tracer — the user confirms impact, places the ball,
and shapes a curve. Milestone 6 adds assisted tracking on top of the same
model, which is why provenance is a first-class field from the start rather
than something retrofitted once tracking exists.
"""
