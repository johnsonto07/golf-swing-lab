# Copy this file to .env and fill in values ONLY if/when you enable optional
# cloud coaching (Milestone 7+). The application works fully without this.
#
# Never commit a real .env file. It is already excluded via .gitignore.

# Optional. Only read by golf_lab/coaching/openai_provider.py, which does
# not exist yet in this milestone. Leave unset for a fully local app.
OPENAI_API_KEY=
